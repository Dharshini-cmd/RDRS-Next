export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";

// Authentication is handled by the RDRS server using a secure HTTP-only
// session cookie. Manus OAuth is no longer used by the application.
